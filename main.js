function changeColor() {
        let line = getElementById('frame');
        let color = line.color;

        if (color == 'darkblue') {
                let color = 'red';
        }

}

document.getElementById("subButton")
        .addEventListener('click',submitClicked);